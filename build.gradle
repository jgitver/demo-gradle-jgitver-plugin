allprojects {
  task hello << { task -> println "In $task.project.name::$task.project.version" }
}

subprojects {

  apply plugin: "java"
 
  repositories {
    mavenCentral()
  }

  dependencies {
     testCompile "junit:junit:4+"
  }
}